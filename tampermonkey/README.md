# Tampermonkey 脚本

`douyin-spark-helper.user.js` 是独立脚本，不依赖服务端。它在抖音网页版私信页面添加本地配置面板：从当前聊天列表读取会话、选择目标、设置消息模板并进行定位验证。

脚本采用浏览器 `localStorage` 保存配置和面板位置，数据只保留在当前浏览器。拖动面板顶部标题栏可移动面板；当页面未自动识别会话时，可输入好友昵称或备注名并点击“添加”。首次使用保持 Dry Run；启用实际发送前，应逐一确认页面选择器仍匹配当前抖音网页版本。
