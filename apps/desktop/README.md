# Spark Helper Desktop Client

Windows 本地配置客户端，使用 Python 标准库 Tkinter，不额外安装 GUI 依赖。

```powershell
python -m apps.desktop.main
```

配置保存到 `data/spark-client.json`。客户端管理好友、文案、任务开关、随机发送窗口和本地执行记录；实际抖音页面操作仍由 Tampermonkey 脚本负责。
